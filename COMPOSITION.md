# TruthTrace merged build

This package combines:
- FRONTEND: `TruthTrace_Palette5_CaseWorkflow_Final_v5_SESSIONFIX(2).zip` (ZIP 1)
- BACKEND: `TruthTrace_AI_Fully_Rebuilt_New_Model_Architecture(1).zip` (ZIP 2)

The root frontend files, `src/`, package configuration, Vite/Tailwind configuration, and frontend documentation come from ZIP 1.
The entire `backend/` directory comes from ZIP 2, including the rebuilt forensic model service, database, upload/report data, requirements, and Docker files.
