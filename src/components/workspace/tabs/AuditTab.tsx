import React,{useEffect,useState} from 'react';
import {Clock3,UserRound,ShieldCheck,ListChecks} from 'lucide-react';
import {Panel,monoClass} from '../../ui/Primitives';
import {api} from '../../../api';
function fmt(v:string){const d=new Date(v);if(Number.isNaN(d.getTime())) return v||'Unknown time';return new Intl.DateTimeFormat('en-GB',{day:'2-digit',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false,timeZone:'Asia/Kolkata'}).format(d)+' IST'}
export function AuditTab({caseId}:{caseId:string}){const[d,setD]=useState<any[]>([]);useEffect(()=>{api.audit(caseId).then(setD)},[caseId]);return <div className="mx-auto max-w-5xl px-8 py-8"><div className="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><div className="text-[10px] font-bold uppercase tracking-[.16em] text-blue-300">DOCUMENTATION / ACCOUNTABILITY</div><h2 className="mt-1 text-lg font-bold text-white">Investigation Audit Trail</h2><p className="mt-1 max-w-2xl text-[13px] leading-6 text-slate-400">A chronological record of backend actions for this case. Each event is shown with its recorded timestamp and actor.</p></div><div className="flex w-fit items-center gap-2 rounded-md border border-blue-400/10 bg-blue-400/[.04] px-3 py-2 text-[11px] text-slate-400"><ShieldCheck size={14} className="text-blue-300"/> All times IST</div></div><Panel className="mt-5 border-white/10 bg-[#0d1830] p-4"><div className="mb-3 flex items-center gap-2 text-[11px] text-slate-500"><ListChecks size={14} className="text-blue-300"/><span>{d.length} recorded event{d.length===1?'':'s'}</span></div><div className="overflow-x-auto rounded-md border border-white/5"><div className="min-w-[760px]"><div className="grid grid-cols-[215px_175px_minmax(330px,1fr)] gap-4 border-b border-white/10 bg-[#091226] px-4 py-3 text-[10px] font-bold uppercase tracking-[.12em] text-slate-500"><span>Timestamp (IST)</span><span>Actor</span><span>Event / Action</span></div>{d.length?d.map((x,i)=><div key={i} className="grid grid-cols-[215px_175px_minmax(330px,1fr)] gap-4 border-b border-white/5 px-4 py-3.5 last:border-0"><div className={`flex items-start gap-2 whitespace-nowrap text-[11px] leading-5 text-slate-300 ${monoClass}`}><Clock3 size={13} className="mt-0.5 shrink-0 text-blue-300"/><span>{fmt(x.t)}</span></div><div className="flex min-w-0 items-start gap-2 text-[12px] leading-5 text-slate-300"><UserRound size={13} className="mt-0.5 shrink-0 text-slate-500"/><span className="break-all">{x.actor||'System'}</span></div><div className="min-w-0 break-words text-[12.5px] leading-6 text-slate-300">{x.e}</div></div>):<div className="p-8 text-center text-sm text-slate-500">No audit events recorded.</div>}</div></div></Panel></div>}
