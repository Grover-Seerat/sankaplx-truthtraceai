import React from 'react';import {FileText,Eye,Download} from 'lucide-react';import {Panel,monoClass} from '../ui/Primitives';import {SimplePage} from './SimplePage';import type {CaseSummary} from '../../types';export function ForensicReportsPage({cases,onPreviewCase}:{cases:CaseSummary[];onPreviewCase:(id:string)=>void}){return <SimplePage title="Forensic Reports" subtitle="Case-specific reports generated from backend evidence." icon={FileText}><div className="flex flex-col gap-2">{cases.map(c=><Panel key={c.id} className="flex items-center justify-between border-white/10 bg-[#0d1830] p-4"><div><div className={`text-[13px] font-semibold text-white ${monoClass}`}>{c.id}</div><div className="text-[12px] text-slate-500">{c.type} · backend generated</div></div><button onClick={()=>onPreviewCase(c.id)} className="flex items-center gap-1.5 text-[13px] font-semibold text-blue-300 hover:text-white"><Eye size={14}/> Preview & Download <Download size={13}/></button></Panel>)}</div></SimplePage>}
